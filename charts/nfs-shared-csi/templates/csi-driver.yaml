apiVersion: storage.k8s.io/v1
kind: CSIDriver
metadata:
  name: {{ include "nfs-shared-csi.driverName" . }}
  labels:
    {{- include "nfs-shared-csi.labels" . | nindent 4 }}
spec:
  attachRequired: false
  podInfoOnMount: false
  fsGroupPolicy: None
  volumeLifecycleModes:
    - Persistent
